# UrbanSolarCarver — Daylight mode
# Carves voxels based on CIE Standard Overcast Sky luminance distribution.
# Sky patches near zenith contribute more than those near the horizon.
# No EPW data is needed — the CIE overcast sky is purely geometric.
# Analysis period is optional (CIE weights are time-invariant).
# Typical use: daylight access envelopes, overcast-sky design.

# --- Required paths ---
max_volume_path:    "path/to/maxVolume.ply"
test_surface_path:  "path/to/testSurfaces.ply"
out_dir:            "path/to/output"

mode: daylight

# Analysis period (full year is typical for daylight)
start_month: 1
start_day:   1
start_hour:  0
end_month:   12
end_day:     31
end_hour:    23

# Grid
voxel_size: 1.0

# Thresholding
threshold: carve_fraction
carve_fraction: 0.7

# -- Defaults you may want to override ----------------------------------------
# score_smoothing: null  # Gaussian blur radius (m) on scores before thresholding. null = auto (1.1× voxel_size). 0 = off. Try 1.0-1.2× voxel_size.
# voxel_size:      1.0    # voxel edge in metres (0.5 = 8x more voxels)
# grid_step:       1.0    # test-surface sampling spacing in metres
# ray_length:      300.0  # max ray distance in metres
# apply_smoothing: false  # true = SDF smooth + marching cubes
# device:          'auto' # 'auto', 'cpu', or 'cuda'
# See REFERENCE_all_options.yaml for the complete parameter list.
